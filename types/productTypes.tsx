export type RemainingTime = {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
};

export enum ProductNavType {
  Auction = "Auction",
  Buy = "Buy Now",
  Promotion = "Promotion",
}
